overwrite: true
schema: "http://localhost:8000/___graphql"
documents:
  - ./src/**/*.{ts,tsx}

generates:
  ./src/graphql-types.d.ts:
    config:
      namingConvention:
        enumValues: keep
    plugins:
      - "typescript"
