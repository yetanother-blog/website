import {
  BackgroundColorProps,
  SpaceProps,
  BorderRadiusProps,
  DisplayProps,
  FlexboxProps,
  MinHeightProps,
  WidthProps,
} from 'styled-system';

export type StyledSystemBoxProps = BackgroundColorProps &
  SpaceProps &
  BorderRadiusProps &
  DisplayProps &
  FlexboxProps &
  MinHeightProps &
  WidthProps;
