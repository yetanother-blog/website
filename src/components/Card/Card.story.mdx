import { Story, Meta, Preview, Props } from '@storybook/addon-docs/blocks';
import { Card } from './Card';
import { ThemeContext } from 'styled-components';
import { Typography } from '../Typography/Typography';
import { Link } from '../Link/Link';
import { Box } from '../Box/Box';

<Meta title="Components|Card" />

# Card

Card component to seperate blog formats.

<Preview>
  <Story name="default">
    <ThemeContext.Consumer>
      {(theme) => (
        <Box
          display="flex"
          flexDirection={['column', 'row']}
          justifyContent="space-between"
          marginBottom={theme.space.m}
        >
          <Card width={['100%', 'calc(50% - 10px)']} to="/" marginBottom={theme.space.l}>
            <Link display="block" variant="primary" component="span" marginBottom={theme.space.m}>
              repo of the week
            </Link>
            <Typography variant="smallText">
              Check out the latest indipendent repository on the internet. We provide you opinionated information.
            </Typography>
          </Card>
          <Card width={['100%', 'calc(50% - 10px)']} to="/" marginBottom={theme.space.l}>
            <Link display="block" variant="secondary" component="span" marginBottom={theme.space.m}>
              guide of the month
            </Link>
            <Typography variant="smallText">
              On a monthly basis we create valueble guides about the latest technologies on the web.
            </Typography>
          </Card>
        </Box>
      )}
    </ThemeContext.Consumer>
  </Story>
</Preview>

## Props

<Props of={Card} />
