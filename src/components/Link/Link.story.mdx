import { Story, Meta, Preview, Props } from '@storybook/addon-docs/blocks';
import { Link } from './Link';
import { ThemeContext } from 'styled-components';
import { Link as GatsbyLink } from 'gatsby';

<Meta title="Components/Link" />

# Link

Link component to wrap pages and posts.

<Preview>
  <Story name="default">
    <ThemeContext.Consumer>
      {(theme) => (
        <React.Fragment>
          <Link
            variant="primary"
            display="block"
            marginBottom={theme.space.xl}
            href="https://twitter.com/_yetanotherblog"
            target="_blank"
          >
            hello link
          </Link>
          <Link variant="secondary" display="block" marginBottom={theme.space.xl} component={GatsbyLink} to="/lol">
            hello link
          </Link>
          <Link variant="tertiary" display="block" marginBottom={theme.space.xl} component={GatsbyLink} to="/lol">
            hello link
          </Link>
          <Link variant="quaternary" display="block" marginBottom={theme.space.xl} component={GatsbyLink} to="/lol">
            hello link
          </Link>
        </React.Fragment>
      )}
    </ThemeContext.Consumer>
  </Story>
</Preview>

## Props

<Props of={Link} />
